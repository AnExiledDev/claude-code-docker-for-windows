{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Claude CLI Debug",
			"type": "node",
			"request": "launch",
			"program": "${workspaceFolder}/node_modules/.bin/claude",
			"console": "integratedTerminal",
			"env": {
				"NODE_ENV": "development"
			}
		}
	]
}